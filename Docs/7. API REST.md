# API REST — Formatos Odontologicos v2.0.0

Base URL: `http://localhost:7070`

---

## Dashboard

| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/dashboard/kpis` | KPIs: ingresos mes/semana, tratamientos en curso, docentes hoy |
| GET | `/api/dashboard/ingresos-mensuales` | Ingresos por mes (12 meses, ano actual) |
| GET | `/api/dashboard/tratamientos-estado` | Conteo de tratamientos por estado (CERRADO/ABIERTO/ANULADO) |
| GET | `/api/dashboard/top-materiales` | Top 5 materiales mas usados en tratamientos cerrados |
| GET | `/api/dashboard/asistencia-hoy` | Lista de docentes con estado de asistencia del dia |

---

## Catalogos

### Materiales
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/materiales` | Listar todos. Query param: `?q=` (busqueda) |
| GET | `/api/catalogos/materiales/{id}` | Buscar por ID |
| POST | `/api/catalogos/materiales` | Crear. Body: `{ nombre, unidad }` |
| PUT | `/api/catalogos/materiales/{id}` | Actualizar. Body: `{ nombre, unidad, estado }` |
| DELETE | `/api/catalogos/materiales/{id}` | Eliminar |

### Docentes
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/docentes` | Listar. Query param: `?q=` |
| POST | `/api/catalogos/docentes` | Crear. Body: `{ nombres, apellidos, telefono }` |
| PUT | `/api/catalogos/docentes/{id}` | Actualizar. Body: `{ nombres, apellidos, telefono, estado }` |
| DELETE | `/api/catalogos/docentes/{id}` | Eliminar |

### Pacientes
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/pacientes` | Listar. Query param: `?q=` |
| POST | `/api/catalogos/pacientes` | Crear. Body: `{ nombres, apellidos }` |
| PUT | `/api/catalogos/pacientes/{id}` | Actualizar. Body: `{ nombres, apellidos, estado }` |
| DELETE | `/api/catalogos/pacientes/{id}` | Eliminar |

### Operadores (Especialistas)
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/operadores` | Listar. Query param: `?q=` |
| POST | `/api/catalogos/operadores` | Crear. Body: `{ nombres, apellidos, dni, grado, tipo, periodo }` |
| PUT | `/api/catalogos/operadores/{id}` | Actualizar. Body: `{ nombres, apellidos, dni, grado, tipo, periodo, estado }` |
| DELETE | `/api/catalogos/operadores/{id}` | Eliminar |

### Tratamientos Predefinidos
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/tratamientos-pred` | Listar. Query param: `?q=` |
| POST | `/api/catalogos/tratamientos-pred` | Crear. Body: `{ nombreTratamiento, montoSugerido }` |
| PUT | `/api/catalogos/tratamientos-pred/{id}` | Actualizar. Body: `{ nombreTratamiento, montoSugerido, estado }` |
| DELETE | `/api/catalogos/tratamientos-pred/{id}` | Eliminar (con transaccion) |
| GET | `/api/catalogos/tratamientos-pred/{id}/materiales` | Materiales de la plantilla |
| PUT | `/api/catalogos/tratamientos-pred/{id}/materiales` | Reemplazar materiales. Body: `{ materiales: [{ materialID, cantidad }] }` |

### Tratamientos Realizados
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/tratamientos/buscar` | Buscar con filtros. Query: `?q=&operador=&paciente=&unidad=&tipo=&estado=` |

### Conversiones de Unidad
| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/catalogos/conversiones` | Listar todas |
| GET | `/api/catalogos/conversiones/material/{id}` | Conversiones de un material |
| POST | `/api/catalogos/conversiones` | Crear. Body: `{ materialID, unidadBase, unidadEmpaque, factor }` |
| PUT | `/api/catalogos/conversiones/{id}` | Actualizar. Body: completo |
| DELETE | `/api/catalogos/conversiones/{id}` | Eliminar |

---

## Tratamientos

| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/tratamientos/activos` | Tratamientos ABIERTO + CERRADO (sin ANULADO) |
| GET | `/api/tratamientos/{id}` | Buscar por ID |
| POST | `/api/tratamientos` | Crear. Body: `{ operadorID, pacienteID, unidadID, fecha, tipo, monto?, tratamientoPredID? }` |
| POST | `/api/tratamientos/{id}/cerrar` | Cerrar tratamiento |
| POST | `/api/tratamientos/{id}/reabrir` | Reabrir tratamiento cerrado |
| POST | `/api/tratamientos/{id}/pago` | Registrar pago. Body: `{ monto }` |
| POST | `/api/tratamientos/{id}/cambiar-tipo` | Cambiar NORMAL↔CONTINUO. Body: `{ tipo }` |
| POST | `/api/tratamientos/{id}/anular` | Anular. Body: `{ motivo }` |
| PUT | `/api/tratamientos/{id}/editar` | Edicion retroactiva (solo cerrados). Body: `{ monto?, gasto?, materiales? }` |
| PUT | `/api/tratamientos/{id}/materiales` | Reemplazar materiales. Body: `{ materiales: [{ materialID, cantidad }] }` |
| POST | `/api/tratamientos/{id}/materiales` | Agregar un material. Body: `{ materialId, cantidad }` |
| PUT | `/api/tratamientos/materiales/{id}/cantidad` | Actualizar cantidad. Body: `{ cantidad }` |
| DELETE | `/api/tratamientos/materiales/{id}` | Quitar material |

---

## Asistencia

| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/asistencia/por-fecha?fecha=YYYY-MM-DD` | Asistencia de todos los docentes en una fecha |
| POST | `/api/asistencia/abrir` | Abrir dia. Body: `{ docenteId, fecha, horaEntrada }` |
| PUT | `/api/asistencia/{id}/entrada` | Editar hora entrada. Body: `{ horaEntrada }` |
| PUT | `/api/asistencia/{id}/salida` | Registrar salida. Body: `{ horaSalida }` |
| PUT | `/api/asistencia/{id}/revertir-salida` | Revertir registro de salida |
| POST | `/api/asistencia/{id}/anular` | Anular. Body: `{ motivo }` |
| GET | `/api/asistencia/{id}/detalle` | Asistencia + ausencias + materiales |
| POST | `/api/asistencia/{id}/acumular-material` | Acumular material. Body: `{ materialId, cantidad }` |
| GET | `/api/asistencia/{id}/materiales` | Materiales del dia |
| POST | `/api/asistencia/{id}/ausencias` | Iniciar ausencia. Body: `{ horaInicio, motivo? }` |
| PUT | `/api/asistencia/{id}/ausencias/{ausId}/regresar` | Registrar regreso. Body: `{ horaFin }` |
| DELETE | `/api/asistencia/{id}/ausencias/{ausId}` | Eliminar periodo de ausencia |
| GET | `/api/asistencia/materiales-default` | Lista predeterminada de materiales para docentes |
| PUT | `/api/asistencia/materiales-default` | Guardar lista predeterminada. Body: `[{ materialId, cantidad }]` |

---

## Reportes

| Metodo | Ruta | Descripcion |
|---|---|---|
| POST | `/api/reportes/materiales/generar` | Generar Excel de materiales. Body: `{ anio, mes, mesFin? }` |
| POST | `/api/reportes/economico/generar` | Generar Excel economico. Body: `{ anio, mes, mesFin? }` |
| POST | `/api/reportes/docente/generar` | Generar Excel de docente. Body: `{ anio, mes }` |
| POST | `/api/reportes/especialista/generar` | Generar Excel de especialista. Body: `{ anio, mes }` |
| POST | `/api/reportes/asistencia/generar` | Generar Excel de asistencia (con horarios y ausencias). Body: `{ anio, mes }` |
| POST | `/api/reportes/semilla/generar` | Generar todos los reportes de una vez |
| GET | `/api/reportes/recientes` | Lista de archivos generados recientemente |
| POST | `/api/reportes/abrir` | Abrir archivo en explorador. Body: `{ path }` |

---

## Unidades

| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/api/unidades` | Listar todas |
| POST | `/api/unidades` | Crear nueva unidad |
| DELETE | `/api/unidades/{id}` | Eliminar (solo si no tiene tratamiento activo) |

---

## Sistema

| Metodo | Ruta | Descripcion |
|---|---|---|
| GET | `/health` | Health check — retorna `{ status: "OK" }` |
| POST | `/shutdown` | Solo disponible en modo dev (`app.env != production`) |

---

## Codigos de error

| Codigo | Significado |
|---|---|
| 200 | OK |
| 201 | Creado |
| 204 | Eliminado (sin contenido) |
| 400 | Error de validacion / negocio |
| 404 | No encontrado |
| 409 | Entidad duplicada |
| 500 | Error interno del servidor |

*Documento generado el 2026-08-11 — 45 endpoints en 7 controladores.*
